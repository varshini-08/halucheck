[pytest]
# Keep pytest artifacts inside the workspace. The Windows global temp root is
# inaccessible in this environment, and the cache provider points at a stale
# inaccessible cache directory.
addopts = --basetemp=.pytest-tmp -p no:cacheprovider
norecursedirs = .pytest-tmp .test-tmp .pytest_cache
