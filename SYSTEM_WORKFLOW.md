# System Workflow

Question → selected LLM answer → atomic claim extraction/filtering → domain routing → local and configured-source retrieval → normalization/deduplication/relevance filtering → batched DeBERTa NLI → transparent metrics → SQLite history/dashboard/export.
