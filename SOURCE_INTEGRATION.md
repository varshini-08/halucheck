# Source Integration

`GET /api/sources` describes registered sources and `GET /api/sources/status` reports their current availability. Local FAISS and Wikipedia are the primary configured sources in the default environment. Wikidata, PubMed, Crossref, OpenAlex, World Bank, Google Fact Check, NASA, NOAA, and other adapters remain registered but require implementation/credentials before they can contribute evidence.
