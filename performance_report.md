# Performance Report

Timing summaries are produced from measured `processing_time_seconds` values in benchmark outputs. Component-level timings are only reported when instrumentation records them; no estimated values are inserted.

For a benchmark output directory, use `evaluation.performance.summarize_timings` to calculate average, minimum, maximum, median, and samples/second.

Cost is reported as unavailable unless verified provider pricing is configured.
